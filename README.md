# BAL_BUS - Система управления автобусными рейсами

Веб-приложение для управления расписанием автобусных рейсов и покупки билетов, построенное на FastAPI с современным Bootstrap интерфейсом.

## Основные возможности

### Для пассажиров (без регистрации):
- 👀 Просмотр расписания рейсов
- 📅 Фильтрация по дате (сегодня, завтра, выбор даты)
- 🎫 Покупка билетов с вводом ФИО и email
- 📧 Получение билета на email (HTML и текстовая версия)

### Для диспетчеров (требуется регистрация):
- 🔐 Регистрация и вход в систему
- ➕ Создание новых рейсов
- ✏️ Редактирование существующих рейсов
- 🗑️ Деактивация рейсов
- 📊 Просмотр всех рейсов с фильтрацией по дате

## Быстрый старт

### 1. Клонирование и установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# База данных (по умолчанию SQLite)
DATABASE_URL=sqlite+aiosqlite:///./balbus.db

# JWT секретный ключ (минимум 32 символа)
SECRET_KEY=your-secret-key-change-in-production-min-32-chars
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Email настройки (для отправки билетов)
SMTP_ENABLED=true
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=587
SMTP_USER=your-email@yandex.ru
SMTP_PASSWORD=your-password-or-app-token
SMTP_FROM_NAME=TICKET_BUS
SMTP_USE_TLS=true
```

**Примечание для Yandex:**
- Для порта 587 используйте `SMTP_USE_TLS=true`
- Для порта 465 используйте `SMTP_USE_TLS=false` и `SMTP_PORT=465`
- Рекомендуется создать пароль приложения в настройках безопасности Yandex
- Убедитесь, что в настройках почты включен доступ для почтовых программ

### 3. Запуск приложения

```bash
python run.py
```

Приложение будет доступно по адресу: **http://localhost:8006**

## Структура проекта

```
BAL_BUS/
├── backend/
│   ├── main.py              # Точка входа FastAPI приложения
│   ├── core/
│   │   ├── config.py        # Настройки приложения
│   │   ├── database.py      # Конфигурация базы данных
│   │   ├── security.py      # Хеширование паролей и JWT
│   │   └── email.py         # Отправка email с билетами
│   ├── models/
│   │   ├── user.py          # Модель пользователя
│   │   ├── trip.py          # Модель рейса
│   │   └── ticket.py        # Модель билета
│   ├── schemas/
│   │   ├── user.py          # Pydantic схемы пользователя
│   │   ├── trip.py          # Pydantic схемы рейса
│   │   └── ticket.py        # Pydantic схемы билета
│   └── api/
│       ├── auth.py          # API аутентификации (регистрация, вход)
│       ├── trips.py         # API управления рейсами
│       ├── tickets.py       # API покупки билетов
│       ├── deps.py          # Зависимости (проверка авторизации)
│       └── seed.py          # Заполнение тестовыми данными
├── templates/
│   ├── base.html            # Базовый шаблон с Bootstrap
│   ├── index.html           # Главная страница (выбор роли)
│   ├── register.html        # Страница регистрации (устарело, используется модальное окно)
│   └── login.html           # Страница входа (устарело, используется модальное окно)
├── static/                  # Статические файлы (если нужны)
├── requirements.txt         # Зависимости Python
├── run.py                   # Скрипт запуска приложения
├── .env                     # Переменные окружения (не в git)
└── balbus.db                # SQLite база данных (создается автоматически)
```

## API Endpoints

### Публичные endpoints:
- `GET /` - Главная страница с выбором роли
- `GET /health` - Проверка здоровья сервиса
- `GET /api/trips/` - Получение списка рейсов (с фильтрами: `origin`, `destination`, `departure_date`)
- `POST /api/tickets/` - Покупка билета (требует: `trip_id`, `full_name`, `email`, `consent_to_processing`)

### Аутентификация:
- `POST /api/auth/register` - Регистрация пользователя (для диспетчеров)
- `POST /api/auth/login` - Вход в систему (получение JWT токена)
- `GET /api/auth/me` - Получение данных текущего пользователя (требует токен)

### Диспетчерские endpoints (требуют JWT токен):
- `POST /api/trips/` - Создание нового рейса
- `PUT /api/trips/{trip_id}` - Обновление рейса
- `DELETE /api/trips/{trip_id}` - Деактивация рейса (soft delete)

### Swagger документация:
- `GET /docs` - Интерактивная документация API (Swagger UI)
- `GET /redoc` - Альтернативная документация API (ReDoc)

## Использование

### Для пассажиров:
1. Откройте http://localhost:8006
2. Нажмите кнопку **"Я пассажир"**
3. Просматривайте расписание рейсов
4. Используйте фильтры: "Сегодня", "Завтра" или выберите дату
5. Нажмите **"Купить"** на нужном рейсе
6. Заполните форму: ФИО, email, поставьте галочку согласия
7. Билет будет отправлен на указанный email

### Для диспетчеров:
1. Откройте http://localhost:8006
2. Нажмите кнопку **"Я диспетчер"**
3. Зарегистрируйтесь или войдите в систему
4. Используйте панель управления для создания и редактирования рейсов:
   - Укажите маршрут (откуда → куда)
   - Установите время отправления и прибытия
   - Установите цену билета
5. Сохраните рейс - он появится в расписании для пассажиров

## Технологии

### Backend:
- **FastAPI** 0.104+ - современный веб-фреймворк
- **SQLAlchemy** 2.0+ - ORM для работы с БД
- **Pydantic** 2.5+ - валидация данных
- **Python-JOSE** - JWT токены для аутентификации
- **Passlib** - хеширование паролей (pbkdf2_sha256)
- **aiosmtplib** - асинхронная отправка email
- **aiosqlite** - асинхронный драйвер для SQLite

### Frontend:
- **Bootstrap 5** - CSS фреймворк
- **Bootstrap Icons** - иконки
- **Jinja2** - шаблонизатор
- **Vanilla JavaScript** - без фреймворков

### База данных:
- **SQLite** (по умолчанию) - для разработки
- **PostgreSQL** (опционально) - для продакшена

## Особенности реализации

### Безопасность:
- Пароли хранятся в хешированном виде (pbkdf2_sha256)
- JWT токены для аутентификации диспетчеров
- Проверка согласия на обработку персональных данных
- CORS middleware для кросс-доменных запросов

### Email отправка:
- HTML и текстовая версии билета
- Красивое оформление с градиентами
- Поддержка Yandex SMTP (порты 587 и 465)
- Возможность использовать муляж (логирование в консоль)

### База данных:
- Автоматическое создание таблиц при запуске
- Мягкое удаление рейсов (флаг `is_active`)
- Индексы для быстрого поиска
- Тестовые данные автоматически загружаются при первом запуске

## Разработка

### Добавление новых функций:
1. Создайте модель в `backend/models/`
2. Создайте схемы в `backend/schemas/`
3. Добавьте API endpoints в `backend/api/`
4. Подключите роутер в `backend/main.py`
5. Обновите фронтенд в `templates/`

### Запуск в режиме разработки:
```bash
python run.py
```
Сервер автоматически перезагружается при изменении кода.

### Миграции базы данных:
При изменении моделей удалите `balbus.db` для автоматического пересоздания схемы.

## Лицензия

Этот проект создан в образовательных целях.
